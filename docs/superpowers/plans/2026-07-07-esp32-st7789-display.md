# ESP32 ST7789 Boot/Status Display Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Show WiFi-provisioning and gateway-connection status text on a 1.54" ST7789 SPI LCD (240x240) wired to the esp32-assistant device, replacing the current total lack of visual feedback.

**Architecture:** A new `display` component wraps ESP-IDF's built-in `esp_lcd` + `esp_lcd_panel_st7789` driver (no managed/registry component needed — `esp_lcd_panel_st7789.h`/`.c` ship in-tree with ESP-IDF 5.4) behind a tiny `display_show(line1, line2)` API. Text layout math (horizontal centering) and an 8x8 bitmap font are split into a separate, host-testable pure-C module, matching this project's existing `ws_protocol`/`provisioning_form` pattern. `main.c` and `provisioning.c` call `display_show()` at each state transition.

**Tech Stack:** ESP-IDF 5.4 built-in `esp_lcd` (SPI panel I/O + ST7789 driver) and `driver/spi_master.h` (already part of every ESP-IDF build, no new managed component dependency).

## Global Constraints

- Target chip: `esp32s3`. ESP-IDF at `~/esp/esp-idf`, source `export.sh` before any `idf.py` call in this branch's build/flash tasks.
- `esp32-assistant` is its own git repo at `/Users/lugon/code/speech-text-transformer/esp32-assistant`, on branch `feat/wifi-provisioning` (already has WiFi provisioning fully implemented and merged into this branch's history) — continue on this same branch, do not create a new one.
- Pins: SCLK=GPIO42, MOSI=GPIO41, DC=GPIO1, RST=GPIO2, BL=GPIO17. No MISO. No CS GPIO (`cs_gpio_num = -1`, tied to GND on the module). BL driven constant-high once at init, no PWM.
- Panel: ST7789, 240x240, no offset assumed (`x_gap=0, y_gap=0`) — to be confirmed on-device in the final task; if the image looks shifted, the fix is `esp_lcd_panel_set_gap()`'s arguments in `display_init()`.
- Font: 8x8 bitmap, ASCII 0x20-0x7E (95 glyphs) — covers all six status strings below plus arbitrary short English server error text (`WSP_EV_ERROR`'s `ev->text` is not a fixed string, so full printable ASCII is needed, not just the fixed strings' own characters).
- A physical device with the ST7789 wired per the pins above is connected at `/dev/cu.usbmodem101`. `idf.py monitor` needs a real TTY unavailable in sandboxed shells — the final task's manual verification must be relayed to the user rather than attempted automatically.
- **Named risk carried into this plan:** the 8x8 font's bitmap data (Task 1) is hand-transcribed from a well-known public-domain reference table. A single wrong byte renders one glyph incorrectly but does not affect any other glyph or any other code path. This is caught by the final task's on-device, per-character visual check — if a specific character looks wrong, the fix is a one-line change to that character's row bytes in `display_font.c`, nothing else.

---

### Task 1: `display_font` — 8x8 bitmap font + text-centering math (host-tested)

**Files:**
- Create: `esp32-assistant/components/display/include/display_font.h`
- Create: `esp32-assistant/components/display/display_font.c`
- Create: `esp32-assistant/test/test_display_font.c`
- Modify: `esp32-assistant/test/Makefile`
- Create: `esp32-assistant/components/display/CMakeLists.txt`

**Interfaces:**
- Produces: `DISPLAY_FONT_GLYPH_WIDTH` (8), `DISPLAY_FONT_GLYPH_HEIGHT` (8), `DISPLAY_FONT_FIRST_CHAR` (0x20), `DISPLAY_FONT_LAST_CHAR` (0x7E) macros; `const uint8_t *display_font_glyph(char c)` (returns 8 bytes, one per pixel row, bit 0 = leftmost pixel, or `NULL` if `c` is outside the supported range); `int display_layout_line(const char *text, int screen_width)` (returns the x pixel to start drawing at for horizontal centering, or -1 if `text` is wider than `screen_width`). Consumed by `display.c` in Task 2.

- [ ] **Step 1: Create `display_font.h`**

```c
#pragma once
#include <stdint.h>
#include <stddef.h>

#define DISPLAY_FONT_GLYPH_WIDTH  8
#define DISPLAY_FONT_GLYPH_HEIGHT 8
#define DISPLAY_FONT_FIRST_CHAR   0x20
#define DISPLAY_FONT_LAST_CHAR    0x7E

// Returns a pointer to the 8-byte bitmap for `c` (one byte per pixel row,
// top row first; within a byte, bit 0 is the leftmost pixel), or NULL if
// `c` is outside [DISPLAY_FONT_FIRST_CHAR, DISPLAY_FONT_LAST_CHAR].
const uint8_t *display_font_glyph(char c);

// Computes the x pixel offset to horizontally center `text` (rendered at
// DISPLAY_FONT_GLYPH_WIDTH px per character, monospace) within a screen of
// width `screen_width` pixels. Returns the offset (>= 0), or -1 if `text`
// is wider than `screen_width`.
int display_layout_line(const char *text, int screen_width);
```

- [ ] **Step 2: Create `display_font.c`**

```c
#include "display_font.h"
#include <string.h>

// 8x8 monospace bitmap font, ASCII 0x20 (space) through 0x7E (~), one row
// per byte (8 rows), bit 0 = leftmost pixel. Public-domain-style basic
// Latin font data.
static const uint8_t s_font8x8[95][8] = {
    {0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00}, // 0x20 ' '
    {0x18,0x3C,0x3C,0x18,0x18,0x00,0x18,0x00}, // 0x21 '!'
    {0x36,0x36,0x00,0x00,0x00,0x00,0x00,0x00}, // 0x22 '"'
    {0x36,0x36,0x7F,0x36,0x7F,0x36,0x36,0x00}, // 0x23 '#'
    {0x0C,0x3E,0x03,0x1E,0x30,0x1F,0x0C,0x00}, // 0x24 '$'
    {0x00,0x63,0x33,0x18,0x0C,0x66,0x63,0x00}, // 0x25 '%'
    {0x1C,0x36,0x1C,0x6E,0x3B,0x33,0x6E,0x00}, // 0x26 '&'
    {0x06,0x06,0x03,0x00,0x00,0x00,0x00,0x00}, // 0x27 '''
    {0x18,0x0C,0x06,0x06,0x06,0x0C,0x18,0x00}, // 0x28 '('
    {0x06,0x0C,0x18,0x18,0x18,0x0C,0x06,0x00}, // 0x29 ')'
    {0x00,0x66,0x3C,0xFF,0x3C,0x66,0x00,0x00}, // 0x2A '*'
    {0x00,0x0C,0x0C,0x3F,0x0C,0x0C,0x00,0x00}, // 0x2B '+'
    {0x00,0x00,0x00,0x00,0x00,0x0C,0x0C,0x06}, // 0x2C ','
    {0x00,0x00,0x00,0x3F,0x00,0x00,0x00,0x00}, // 0x2D '-'
    {0x00,0x00,0x00,0x00,0x00,0x0C,0x0C,0x00}, // 0x2E '.'
    {0x60,0x30,0x18,0x0C,0x06,0x03,0x01,0x00}, // 0x2F '/'
    {0x3E,0x63,0x73,0x7B,0x6F,0x67,0x3E,0x00}, // 0x30 '0'
    {0x0C,0x0E,0x0C,0x0C,0x0C,0x0C,0x3F,0x00}, // 0x31 '1'
    {0x1E,0x33,0x30,0x1C,0x06,0x33,0x3F,0x00}, // 0x32 '2'
    {0x1E,0x33,0x30,0x1C,0x30,0x33,0x1E,0x00}, // 0x33 '3'
    {0x38,0x3C,0x36,0x33,0x7F,0x30,0x78,0x00}, // 0x34 '4'
    {0x3F,0x03,0x1F,0x30,0x30,0x33,0x1E,0x00}, // 0x35 '5'
    {0x1C,0x06,0x03,0x1F,0x33,0x33,0x1E,0x00}, // 0x36 '6'
    {0x3F,0x33,0x30,0x18,0x0C,0x0C,0x0C,0x00}, // 0x37 '7'
    {0x1E,0x33,0x33,0x1E,0x33,0x33,0x1E,0x00}, // 0x38 '8'
    {0x1E,0x33,0x33,0x3E,0x30,0x18,0x0E,0x00}, // 0x39 '9'
    {0x00,0x0C,0x0C,0x00,0x00,0x0C,0x0C,0x00}, // 0x3A ':'
    {0x00,0x0C,0x0C,0x00,0x00,0x0C,0x0C,0x06}, // 0x3B ';'
    {0x18,0x0C,0x06,0x03,0x06,0x0C,0x18,0x00}, // 0x3C '<'
    {0x00,0x00,0x3F,0x00,0x00,0x3F,0x00,0x00}, // 0x3D '='
    {0x06,0x0C,0x18,0x30,0x18,0x0C,0x06,0x00}, // 0x3E '>'
    {0x1E,0x33,0x30,0x18,0x0C,0x00,0x0C,0x00}, // 0x3F '?'
    {0x3E,0x63,0x7B,0x7B,0x7B,0x03,0x1E,0x00}, // 0x40 '@'
    {0x0C,0x1E,0x33,0x33,0x3F,0x33,0x33,0x00}, // 0x41 'A'
    {0x3F,0x66,0x66,0x3E,0x66,0x66,0x3F,0x00}, // 0x42 'B'
    {0x3C,0x66,0x03,0x03,0x03,0x66,0x3C,0x00}, // 0x43 'C'
    {0x1F,0x36,0x66,0x66,0x66,0x36,0x1F,0x00}, // 0x44 'D'
    {0x7F,0x46,0x16,0x1E,0x16,0x46,0x7F,0x00}, // 0x45 'E'
    {0x7F,0x46,0x16,0x1E,0x16,0x06,0x0F,0x00}, // 0x46 'F'
    {0x3C,0x66,0x03,0x03,0x73,0x66,0x7C,0x00}, // 0x47 'G'
    {0x33,0x33,0x33,0x3F,0x33,0x33,0x33,0x00}, // 0x48 'H'
    {0x1E,0x0C,0x0C,0x0C,0x0C,0x0C,0x1E,0x00}, // 0x49 'I'
    {0x78,0x30,0x30,0x30,0x33,0x33,0x1E,0x00}, // 0x4A 'J'
    {0x67,0x66,0x36,0x1E,0x36,0x66,0x67,0x00}, // 0x4B 'K'
    {0x0F,0x06,0x06,0x06,0x46,0x66,0x7F,0x00}, // 0x4C 'L'
    {0x63,0x77,0x7F,0x7F,0x6B,0x63,0x63,0x00}, // 0x4D 'M'
    {0x63,0x67,0x6F,0x7B,0x73,0x63,0x63,0x00}, // 0x4E 'N'
    {0x1C,0x36,0x63,0x63,0x63,0x36,0x1C,0x00}, // 0x4F 'O'
    {0x3F,0x66,0x66,0x3E,0x06,0x06,0x0F,0x00}, // 0x50 'P'
    {0x1E,0x33,0x33,0x33,0x3B,0x1E,0x38,0x00}, // 0x51 'Q'
    {0x3F,0x66,0x66,0x3E,0x36,0x66,0x67,0x00}, // 0x52 'R'
    {0x1E,0x33,0x07,0x0E,0x38,0x33,0x1E,0x00}, // 0x53 'S'
    {0x3F,0x2D,0x0C,0x0C,0x0C,0x0C,0x1E,0x00}, // 0x54 'T'
    {0x33,0x33,0x33,0x33,0x33,0x33,0x3F,0x00}, // 0x55 'U'
    {0x33,0x33,0x33,0x33,0x33,0x1E,0x0C,0x00}, // 0x56 'V'
    {0x63,0x63,0x63,0x6B,0x7F,0x77,0x63,0x00}, // 0x57 'W'
    {0x63,0x63,0x36,0x1C,0x1C,0x36,0x63,0x00}, // 0x58 'X'
    {0x33,0x33,0x33,0x1E,0x0C,0x0C,0x1E,0x00}, // 0x59 'Y'
    {0x7F,0x63,0x31,0x18,0x4C,0x66,0x7F,0x00}, // 0x5A 'Z'
    {0x1E,0x06,0x06,0x06,0x06,0x06,0x1E,0x00}, // 0x5B '['
    {0x03,0x06,0x0C,0x18,0x30,0x60,0x40,0x00}, // 0x5C '\'
    {0x1E,0x18,0x18,0x18,0x18,0x18,0x1E,0x00}, // 0x5D ']'
    {0x08,0x1C,0x36,0x63,0x00,0x00,0x00,0x00}, // 0x5E '^'
    {0x00,0x00,0x00,0x00,0x00,0x00,0x00,0xFF}, // 0x5F '_'
    {0x0C,0x0C,0x18,0x00,0x00,0x00,0x00,0x00}, // 0x60 '`'
    {0x00,0x00,0x1E,0x30,0x3E,0x33,0x6E,0x00}, // 0x61 'a'
    {0x07,0x06,0x06,0x3E,0x66,0x66,0x3B,0x00}, // 0x62 'b'
    {0x00,0x00,0x1E,0x33,0x03,0x33,0x1E,0x00}, // 0x63 'c'
    {0x38,0x30,0x30,0x3E,0x33,0x33,0x6E,0x00}, // 0x64 'd'
    {0x00,0x00,0x1E,0x33,0x3F,0x03,0x1E,0x00}, // 0x65 'e'
    {0x1C,0x36,0x06,0x0F,0x06,0x06,0x0F,0x00}, // 0x66 'f'
    {0x00,0x00,0x6E,0x33,0x33,0x3E,0x30,0x1F}, // 0x67 'g'
    {0x07,0x06,0x36,0x6E,0x66,0x66,0x67,0x00}, // 0x68 'h'
    {0x0C,0x00,0x0E,0x0C,0x0C,0x0C,0x1E,0x00}, // 0x69 'i'
    {0x30,0x00,0x30,0x30,0x30,0x33,0x33,0x1E}, // 0x6A 'j'
    {0x07,0x06,0x66,0x36,0x1E,0x36,0x67,0x00}, // 0x6B 'k'
    {0x0E,0x0C,0x0C,0x0C,0x0C,0x0C,0x1E,0x00}, // 0x6C 'l'
    {0x00,0x00,0x33,0x7F,0x7F,0x6B,0x63,0x00}, // 0x6D 'm'
    {0x00,0x00,0x1F,0x33,0x33,0x33,0x33,0x00}, // 0x6E 'n'
    {0x00,0x00,0x1E,0x33,0x33,0x33,0x1E,0x00}, // 0x6F 'o'
    {0x00,0x00,0x3B,0x66,0x66,0x3E,0x06,0x0F}, // 0x70 'p'
    {0x00,0x00,0x6E,0x33,0x33,0x3E,0x30,0x78}, // 0x71 'q'
    {0x00,0x00,0x3B,0x6E,0x66,0x06,0x0F,0x00}, // 0x72 'r'
    {0x00,0x00,0x3E,0x03,0x1E,0x30,0x1F,0x00}, // 0x73 's'
    {0x08,0x0C,0x3E,0x0C,0x0C,0x2C,0x18,0x00}, // 0x74 't'
    {0x00,0x00,0x33,0x33,0x33,0x33,0x6E,0x00}, // 0x75 'u'
    {0x00,0x00,0x33,0x33,0x33,0x1E,0x0C,0x00}, // 0x76 'v'
    {0x00,0x00,0x63,0x6B,0x7F,0x7F,0x36,0x00}, // 0x77 'w'
    {0x00,0x00,0x63,0x36,0x1C,0x36,0x63,0x00}, // 0x78 'x'
    {0x00,0x00,0x33,0x33,0x33,0x3E,0x30,0x1F}, // 0x79 'y'
    {0x00,0x00,0x3F,0x19,0x0C,0x26,0x3F,0x00}, // 0x7A 'z'
    {0x38,0x0C,0x0C,0x07,0x0C,0x0C,0x38,0x00}, // 0x7B '{'
    {0x18,0x18,0x18,0x00,0x18,0x18,0x18,0x00}, // 0x7C '|'
    {0x07,0x0C,0x0C,0x38,0x0C,0x0C,0x07,0x00}, // 0x7D '}'
    {0x6E,0x3B,0x00,0x00,0x00,0x00,0x00,0x00}, // 0x7E '~'
};

const uint8_t *display_font_glyph(char c) {
    unsigned char uc = (unsigned char)c;
    if (uc < DISPLAY_FONT_FIRST_CHAR || uc > DISPLAY_FONT_LAST_CHAR) return NULL;
    return s_font8x8[uc - DISPLAY_FONT_FIRST_CHAR];
}

int display_layout_line(const char *text, int screen_width) {
    int len = (int)strlen(text);
    int text_width = len * DISPLAY_FONT_GLYPH_WIDTH;
    if (text_width > screen_width) return -1;
    return (screen_width - text_width) / 2;
}
```

- [ ] **Step 3: Write the failing test — create `test/test_display_font.c`**

```c
#include "display_font.h"
#include <string.h>
#include <stdio.h>

static int failures = 0;
#define CHECK(cond) do { if (!(cond)) { \
  printf("FAIL %s:%d: %s\n", __FILE__, __LINE__, #cond); failures++; } } while (0)

static void test_glyph_space(void) {
    const uint8_t *g = display_font_glyph(' ');
    CHECK(g != NULL);
    for (int i = 0; i < 8; i++) CHECK(g[i] == 0x00);
}

static void test_glyph_known_letter(void) {
    const uint8_t *a = display_font_glyph('A');
    CHECK(a != NULL);
    CHECK(a[0] == 0x0C);
    CHECK(a[7] == 0x00);
}

static void test_glyph_first_and_last(void) {
    CHECK(display_font_glyph((char)0x20) != NULL);
    CHECK(display_font_glyph((char)0x7E) != NULL);
}

static void test_glyph_out_of_range(void) {
    CHECK(display_font_glyph((char)0x1F) == NULL);
    CHECK(display_font_glyph((char)0x7F) == NULL);
}

static void test_glyph_distinct(void) {
    const uint8_t *a = display_font_glyph('A');
    const uint8_t *b = display_font_glyph('B');
    CHECK(memcmp(a, b, 8) != 0);
}

static void test_layout_centers_short_text(void) {
    // "OK" is 2 chars * 8px = 16px wide; centered in 240px -> (240-16)/2 = 112
    CHECK(display_layout_line("OK", 240) == 112);
}

static void test_layout_centers_empty_string(void) {
    CHECK(display_layout_line("", 240) == 120);
}

static void test_layout_exact_fit(void) {
    // 30 chars * 8px = 240px, exactly fills a 240px screen -> offset 0
    CHECK(display_layout_line("123456789012345678901234567890", 240) == 0);
}

static void test_layout_too_wide(void) {
    // 31 chars * 8px = 248px > 240px screen
    CHECK(display_layout_line("1234567890123456789012345678901", 240) == -1);
}

int main(void) {
    test_glyph_space();
    test_glyph_known_letter();
    test_glyph_first_and_last();
    test_glyph_out_of_range();
    test_glyph_distinct();
    test_layout_centers_short_text();
    test_layout_centers_empty_string();
    test_layout_exact_fit();
    test_layout_too_wide();
    if (failures) { printf("%d FAILURES\n", failures); return 1; }
    printf("ALL PASS\n");
    return 0;
}
```

- [ ] **Step 4: Update `test/Makefile` to add the new test target**

```makefile
CC ?= cc
CFLAGS = -std=c11 -Wall -Wextra -g -O0 -I../components/ws_protocol/include \
         -I../components/provisioning/include -I../components/wifi/include \
         -I../components/display/include
SRC_WS_PROTOCOL = ../components/ws_protocol/ws_protocol.c
SRC_PROVISIONING_SSID = ../components/provisioning/provisioning_ssid.c
SRC_PROVISIONING_FORM = ../components/provisioning/provisioning_form.c
SRC_DISPLAY_FONT = ../components/display/display_font.c

.PHONY: test
test: test_ws_protocol test_provisioning_ssid test_provisioning_form test_display_font
	./test_ws_protocol
	./test_provisioning_ssid
	./test_provisioning_form
	./test_display_font

test_ws_protocol: test_ws_protocol.c $(SRC_WS_PROTOCOL)
	$(CC) $(CFLAGS) -o $@ $^

test_provisioning_ssid: test_provisioning_ssid.c $(SRC_PROVISIONING_SSID)
	$(CC) $(CFLAGS) -o $@ $^

test_provisioning_form: test_provisioning_form.c $(SRC_PROVISIONING_FORM)
	$(CC) $(CFLAGS) -o $@ $^

test_display_font: test_display_font.c $(SRC_DISPLAY_FONT)
	$(CC) $(CFLAGS) -o $@ $^

clean:
	rm -rf test_ws_protocol test_ws_protocol.dSYM \
	       test_provisioning_ssid test_provisioning_ssid.dSYM \
	       test_provisioning_form test_provisioning_form.dSYM \
	       test_display_font test_display_font.dSYM
```

- [ ] **Step 5: Run the test**

Run: `cd /Users/lugon/code/speech-text-transformer/esp32-assistant/test && make test_display_font && ./test_display_font`
Expected: `ALL PASS`. If `test_glyph_distinct` or `test_glyph_known_letter` fail, check the transcription of the specific row bytes for the character in question in `display_font.c` — this is the one place a hand-transcription slip could land, per this plan's Global Constraints note.

- [ ] **Step 6: Create `components/display/CMakeLists.txt` (minimal now, gains `display.c` in Task 2)**

```cmake
idf_component_register(
    SRCS "display_font.c" "display.c"
    INCLUDE_DIRS "include"
    REQUIRES esp_lcd driver)
```

(This references `display.c` which doesn't exist until Task 2 — the ESP-IDF build isn't run until Task 5, so this is safe to write now.)

- [ ] **Step 7: Commit**

```bash
git -C /Users/lugon/code/speech-text-transformer/esp32-assistant add \
  components/display/include/display_font.h components/display/display_font.c \
  components/display/CMakeLists.txt test/test_display_font.c test/Makefile
git -C /Users/lugon/code/speech-text-transformer/esp32-assistant commit -m "$(cat <<'EOF'
feat(display): add host-tested 8x8 font + text-centering math

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 2: `display` — SPI + ST7789 panel driver, `display_show()`

**Files:**
- Create: `esp32-assistant/components/display/include/display.h`
- Create: `esp32-assistant/components/display/display.c`

**Interfaces:**
- Consumes: `display_font_glyph()`, `display_layout_line()`, `DISPLAY_FONT_GLYPH_WIDTH`/`HEIGHT` (Task 1).
- Produces: `display_init(void)` → `esp_err_t`; `display_show(const char *line1, const char *line2)` → `void` (`line2` may be `NULL` for a single-line message). Consumed by `main.c` (Task 3) and `provisioning.c` (Task 4).

Not host-testable (ESP-IDF `esp_lcd`/`driver/spi_master.h` throughout). Verified on-device in Task 5.

- [ ] **Step 1: Create `display.h`**

```c
#pragma once
#include "esp_err.h"

// Brings up the SPI bus + ST7789 panel (SCLK=42, MOSI=41, DC=1, RST=2,
// no CS/MISO, 240x240, no offset) and turns the backlight (GPIO17) on.
// Clears the screen to black. Must be called once before display_show().
esp_err_t display_init(void);

// Clears the screen and draws up to two lines of text, each horizontally
// centered, using the 8x8 font. `line2` may be NULL to show a single line
// (vertically centered on its own in that case). Any line wider than the
// screen is silently skipped (not drawn) rather than wrapped or truncated
// mid-character.
void display_show(const char *line1, const char *line2);
```

- [ ] **Step 2: Create `display.c`**

```c
#include "display.h"
#include "display_font.h"
#include "esp_lcd_panel_io.h"
#include "esp_lcd_panel_vendor.h"
#include "esp_lcd_panel_ops.h"
#include "esp_lcd_panel_st7789.h"
#include "esp_lcd_io_spi.h"
#include "driver/spi_master.h"
#include "driver/gpio.h"
#include "esp_log.h"
#include <string.h>

static const char *TAG = "display";

#define DISP_SPI_HOST   SPI2_HOST
#define DISP_SCLK_GPIO  42
#define DISP_MOSI_GPIO  41
#define DISP_DC_GPIO    1
#define DISP_RST_GPIO   2
#define DISP_BL_GPIO    17
#define DISP_WIDTH      240
#define DISP_HEIGHT     240

static esp_lcd_panel_handle_t s_panel;

static void clear_screen(void) {
    uint16_t black_row[DISP_WIDTH];
    memset(black_row, 0, sizeof black_row);
    for (int y = 0; y < DISP_HEIGHT; y++) {
        esp_lcd_panel_draw_bitmap(s_panel, 0, y, DISP_WIDTH, y + 1, black_row);
    }
}

static void draw_char(int x, int y, char c) {
    const uint8_t *glyph = display_font_glyph(c);
    if (!glyph) return;
    uint16_t px[DISPLAY_FONT_GLYPH_WIDTH * DISPLAY_FONT_GLYPH_HEIGHT];
    for (int row = 0; row < DISPLAY_FONT_GLYPH_HEIGHT; row++) {
        uint8_t bits = glyph[row];
        for (int col = 0; col < DISPLAY_FONT_GLYPH_WIDTH; col++) {
            bool on = (bits >> col) & 1;
            px[row * DISPLAY_FONT_GLYPH_WIDTH + col] = on ? 0xFFFF : 0x0000;
        }
    }
    esp_lcd_panel_draw_bitmap(s_panel, x, y, x + DISPLAY_FONT_GLYPH_WIDTH,
                               y + DISPLAY_FONT_GLYPH_HEIGHT, px);
}

static void draw_line(const char *text, int y) {
    int x = display_layout_line(text, DISP_WIDTH);
    if (x < 0) return;  // too wide for the screen — skip rather than wrap
    for (const char *p = text; *p; p++) {
        draw_char(x, y, *p);
        x += DISPLAY_FONT_GLYPH_WIDTH;
    }
}

esp_err_t display_init(void) {
    gpio_config_t bl_cfg = {
        .pin_bit_mask = 1ULL << DISP_BL_GPIO,
        .mode = GPIO_MODE_OUTPUT,
    };
    ESP_ERROR_CHECK(gpio_config(&bl_cfg));
    gpio_set_level(DISP_BL_GPIO, 1);

    spi_bus_config_t buscfg = {
        .mosi_io_num = DISP_MOSI_GPIO,
        .miso_io_num = -1,
        .sclk_io_num = DISP_SCLK_GPIO,
        .quadwp_io_num = -1,
        .quadhd_io_num = -1,
        .max_transfer_sz = DISP_WIDTH * DISPLAY_FONT_GLYPH_HEIGHT * 2,
    };
    ESP_ERROR_CHECK(spi_bus_initialize(DISP_SPI_HOST, &buscfg, SPI_DMA_CH_AUTO));

    esp_lcd_panel_io_handle_t io_handle;
    esp_lcd_panel_io_spi_config_t io_config = {
        .cs_gpio_num = -1,
        .dc_gpio_num = DISP_DC_GPIO,
        .spi_mode = 0,
        .pclk_hz = 20 * 1000 * 1000,
        .trans_queue_depth = 10,
        .lcd_cmd_bits = 8,
        .lcd_param_bits = 8,
    };
    ESP_ERROR_CHECK(esp_lcd_new_panel_io_spi((esp_lcd_spi_bus_handle_t)DISP_SPI_HOST,
                                              &io_config, &io_handle));

    esp_lcd_panel_dev_config_t panel_config = {
        .reset_gpio_num = DISP_RST_GPIO,
        .rgb_ele_order = LCD_RGB_ELEMENT_ORDER_RGB,
        .bits_per_pixel = 16,
    };
    ESP_ERROR_CHECK(esp_lcd_new_panel_st7789(io_handle, &panel_config, &s_panel));

    ESP_ERROR_CHECK(esp_lcd_panel_reset(s_panel));
    ESP_ERROR_CHECK(esp_lcd_panel_init(s_panel));
    ESP_ERROR_CHECK(esp_lcd_panel_set_gap(s_panel, 0, 0));
    ESP_ERROR_CHECK(esp_lcd_panel_disp_on_off(s_panel, true));

    clear_screen();
    ESP_LOGI(TAG, "display ready");
    return ESP_OK;
}

void display_show(const char *line1, const char *line2) {
    clear_screen();
    if (line2 == NULL) {
        draw_line(line1, (DISP_HEIGHT - DISPLAY_FONT_GLYPH_HEIGHT) / 2);
    } else {
        int gap = 4;
        int total_h = 2 * DISPLAY_FONT_GLYPH_HEIGHT + gap;
        int y1 = (DISP_HEIGHT - total_h) / 2;
        int y2 = y1 + DISPLAY_FONT_GLYPH_HEIGHT + gap;
        draw_line(line1, y1);
        draw_line(line2, y2);
    }
}
```

- [ ] **Step 3: Commit**

```bash
git -C /Users/lugon/code/speech-text-transformer/esp32-assistant add \
  components/display/include/display.h components/display/display.c
git -C /Users/lugon/code/speech-text-transformer/esp32-assistant commit -m "$(cat <<'EOF'
feat(display): add ST7789 SPI panel driver and display_show()

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 3: wire `display` into `main.c`

**Files:**
- Modify: `esp32-assistant/main/main.c`
- Modify: `esp32-assistant/main/CMakeLists.txt`

**Interfaces:**
- Consumes: `display_init()`, `display_show(line1, line2)` (Task 2).

- [ ] **Step 1: Add the includes**

Add `#include "display.h"` to `main/main.c`, right after the existing `#include "provisioning.h"` line (main.c:3). Also add `#include <stdio.h>` next to the existing `#include <string.h>`/`#include <stdlib.h>` lines (main.c:12-13) — needed for the `snprintf` calls added in this task (Step 3 below and Task 4's `provisioning.c` change both use it; `main.c` doesn't currently pull in `<stdio.h>` transitively).

- [ ] **Step 2: Call `display_init()` at the top of `app_main`, and add status updates at each state transition**

Replace `app_main`'s body (currently `main/main.c:79-114`) with:

```c
void app_main(void) {
    ESP_LOGI(TAG, "esp32-assistant booting");
    ESP_ERROR_CHECK(display_init());

    esp_err_t nvs_err = nvs_flash_init();
    if (nvs_err == ESP_ERR_NVS_NO_FREE_PAGES || nvs_err == ESP_ERR_NVS_NEW_VERSION_FOUND) {
        ESP_ERROR_CHECK(nvs_flash_erase());
        ESP_ERROR_CHECK(nvs_flash_init());
    }

    wifi_cfg_t cfg;
    ESP_ERROR_CHECK(wifi_cfg_load(&cfg));

    display_show("Connecting WiFi...", NULL);
    ESP_ERROR_CHECK(wifi_sta_start(cfg.ssid, cfg.password));
    if (!wifi_sta_wait_connected(15000)) {
        ESP_LOGW(TAG, "wifi connect failed, starting provisioning portal");
        display_show("WiFi failed", "Starting setup AP...");
        provisioning_start(&cfg);  // does not return
    }

    display_show("WiFi OK", "Connecting gateway...");
    ESP_ERROR_CHECK(audio_init());
    ESP_ERROR_CHECK(opus_codec_init());

    s_pktq = xQueueCreate(16, sizeof(pkt_t *));   // ~16*60ms buffer ceiling

    wsp_config_t wcfg = {
        .host = cfg.server_host, .port = cfg.server_port,
        .secure = CONFIG_AA_SERVER_SECURE,
        .stt_engine = CONFIG_AA_STT_ENGINE, .tts_engine = CONFIG_AA_TTS_ENGINE,
        .language = CONFIG_AA_LANGUAGE, .sample_rate = 16000, .output_sample_rate = 16000,
        .profile = CONFIG_AA_PROFILE,
    };
    ESP_ERROR_CHECK(ws_client_start(&wcfg, on_event, on_audio));

    xTaskCreatePinnedToCore(spk_task, "spk", 4096, NULL, 6, NULL, 1);
    xTaskCreatePinnedToCore(mic_task, "mic", 4096, NULL, 5, NULL, 1);
    ESP_LOGI(TAG, "running");
}
```

- [ ] **Step 3: Show status on session-ready and error events in `on_event`**

Modify the `WSP_EV_SESSION_STARTED` and `WSP_EV_ERROR` cases in `on_event` (currently `main/main.c:32` and `main/main.c:41`) to add a `display_show()` call. The full updated function:

```c
static void on_event(const wsp_event_t *ev) {
    switch (ev->type) {
    case WSP_EV_SESSION_STARTED: {
        s_state = APP_LISTENING;
        ESP_LOGI(TAG, "session ready");
        char host_port[128 + 1 + 6];  // host + ':' + up to 5-digit port + NUL
        snprintf(host_port, sizeof host_port, "%s:%d", s_wcfg_host, s_wcfg_port);
        display_show("Connected", host_port);
        break;
    }
    case WSP_EV_USER_TRANSCRIPT: ESP_LOGI(TAG, "you: %s", ev->text); break;
    case WSP_EV_RESPONSE_TEXT:   ESP_LOGI(TAG, "bot: %s", ev->text); break;
    case WSP_EV_AUDIO_START:     s_state = APP_SPEAKING; break;
    case WSP_EV_TURN_DONE:       s_state = APP_LISTENING; break;
    case WSP_EV_ABORTED:
        s_state = APP_LISTENING;
        { pkt_t *p; while (xQueueReceive(s_pktq, &p, 0) == pdTRUE) free(p); }  // flush
        break;
    case WSP_EV_ERROR:
        ESP_LOGE(TAG, "server error: %s", ev->text);
        display_show("Error", ev->text);
        break;
    default: break;
    }
}
```

This references two new statics, `s_wcfg_host`/`s_wcfg_port`, that don't exist yet — add them right above `on_event` (before `main/main.c:30`, where `on_event` is currently defined) and set them from `wcfg` in `app_main` right after building `wcfg`:

```c
// Set once in app_main before ws_client_start(); read by on_event() to show
// "host:port" on the session-ready screen without threading cfg through the callback.
// Sized to match wifi_cfg_t.server_host (WIFI_CFG_HOST_MAX=127 + NUL), so a
// long configured hostname isn't silently truncated on the status screen.
static char s_wcfg_host[128];
static int  s_wcfg_port;
```

Add this snprintf right after the `wsp_config_t wcfg = { ... };` initializer in `app_main` (before the `ESP_ERROR_CHECK(ws_client_start(&wcfg, on_event, on_audio));` line):

```c
    strncpy(s_wcfg_host, cfg.server_host, sizeof(s_wcfg_host) - 1);
    s_wcfg_port = cfg.server_port;
```

- [ ] **Step 4: Update `main/CMakeLists.txt`**

```cmake
idf_component_register(
    SRCS "main.c"
    INCLUDE_DIRS "."
    REQUIRES wifi ws_protocol ws_client audio opus_codec nvs_flash provisioning display)
```

- [ ] **Step 5: Commit**

```bash
git -C /Users/lugon/code/speech-text-transformer/esp32-assistant add main/main.c main/CMakeLists.txt
git -C /Users/lugon/code/speech-text-transformer/esp32-assistant commit -m "$(cat <<'EOF'
feat(main): show WiFi/gateway connection status on the ST7789 display

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 4: wire `display` into `provisioning.c`

**Files:**
- Modify: `esp32-assistant/components/provisioning/provisioning.c`
- Modify: `esp32-assistant/components/provisioning/CMakeLists.txt`

**Interfaces:**
- Consumes: `display_show(line1, line2)` (Task 2). Note: `display_init()` is called once in `main.c` (Task 3) before either `wifi_sta_start()` or `provisioning_start()` run, so `provisioning.c` only ever calls `display_show()`, never `display_init()`.

- [ ] **Step 1: Add the include**

Add `#include "display.h"` to `components/provisioning/provisioning.c`, in the include block near the top (after `#include "provisioning_form.h"`).

- [ ] **Step 2: Show the AP SSID + IP once the SoftAP is up**

In `provisioning_start()`, right after the existing line:

```c
    ESP_LOGI(TAG, "provisioning AP '%s' up at 192.168.9.1", ssid);
```

add:

```c
    char ssid_ip[64];
    snprintf(ssid_ip, sizeof ssid_ip, "%s 192.168.9.1", ssid);
    display_show("Setup WiFi", ssid_ip);
```

(`snprintf` needs `#include <stdio.h>` — `provisioning.c` does not currently include it; add `#include <stdio.h>` alongside the other includes at the top of the file.)

- [ ] **Step 3: Update `components/provisioning/CMakeLists.txt`**

```cmake
idf_component_register(
    SRCS "provisioning_ssid.c" "provisioning_form.c" "provisioning.c"
    INCLUDE_DIRS "include"
    REQUIRES wifi esp_wifi esp_netif esp_http_server nvs_flash lwip display)
```

- [ ] **Step 4: Commit**

```bash
git -C /Users/lugon/code/speech-text-transformer/esp32-assistant add \
  components/provisioning/provisioning.c components/provisioning/CMakeLists.txt
git -C /Users/lugon/code/speech-text-transformer/esp32-assistant commit -m "$(cat <<'EOF'
feat(provisioning): show AP SSID + IP on the ST7789 display during setup

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 5: build, flash, and verify all six display states on the connected device

**Files:** none (build/flash/manual verification only).

- [ ] **Step 1: Run host tests**

Run: `cd /Users/lugon/code/speech-text-transformer/esp32-assistant/test && make clean && make test`
Expected: `ALL PASS` for all four test binaries (`test_ws_protocol`, `test_provisioning_ssid`, `test_provisioning_form`, `test_display_font`).

- [ ] **Step 2: Build the firmware**

Run:
```bash
source ~/esp/esp-idf/export.sh
idf.py -C /Users/lugon/code/speech-text-transformer/esp32-assistant build
```
Expected: `Project build complete.` with no errors. If it fails on unresolved `esp_lcd_panel_*` symbols, check `components/display/CMakeLists.txt`'s `REQUIRES` includes `esp_lcd` and `driver`.

- [ ] **Step 3: Confirm the device is connected**

Run: `ls /dev/cu.usbmodem*`
Expected: `/dev/cu.usbmodem101` (adjust the port in later steps if it enumerates differently).

- [ ] **Step 4: Flash**

Run:
```bash
source ~/esp/esp-idf/export.sh
idf.py -C /Users/lugon/code/speech-text-transformer/esp32-assistant -p /dev/cu.usbmodem101 flash
```
Expected: `Hash of data verified.` for all images, ending in `Hard resetting via RTS pin... Done`. If it fails with "port is busy", check for a leftover `idf.py monitor` process (`lsof /dev/cu.usbmodem101`) and ask the user to close it.

- [ ] **Step 5: Manually verify each display state with the user (cannot be automated from the sandboxed shell — report exact results back)**

Ask the user to confirm, in order:
1. **Right after boot, before WiFi connects:** screen shows `"Connecting WiFi..."` — readable, centered.
2. **If WiFi isn't yet configured (or times out):** screen shows `"WiFi failed"` / `"Starting setup AP..."`, then the SoftAP comes up and the screen shows `"Setup WiFi"` / `"<SSID> 192.168.9.1"` with the actual `Lugo-XXXX` SSID.
3. **Once WiFi connects (already-provisioned device):** screen briefly shows `"WiFi OK"` / `"Connecting gateway..."`.
4. **Once the gateway session starts:** screen shows `"Connected"` / `"<host>:<port>"` matching the configured gateway.
5. **If the gateway sends a WS error:** screen shows `"Error"` / the error text (may be truncated if long — that's expected per this plan's non-goals, no wrapping).
6. **Orientation/legibility check:** is the text upright (not upside-down or mirrored)? Are all characters legible, or does any specific character look wrong (this would point at a transcription slip in `display_font.c`'s row bytes for that character — see this plan's Global Constraints note)? Is the image centered on the panel, or does it look shifted (would indicate `esp_lcd_panel_set_gap()` in `display_init()` needs non-zero values for this specific panel)?

- [ ] **Step 6: If anything in Step 5 looks wrong, debug with systematic-debugging**

If text is upside-down/mirrored, garbled, or shifted, invoke the `superpowers:systematic-debugging` skill rather than guessing — likely causes to investigate methodically: `esp_lcd_panel_mirror()`/`esp_lcd_panel_swap_xy()` flags for orientation, `esp_lcd_panel_set_gap()` for offset, `LCD_RGB_ELEMENT_ORDER_RGB` vs `_BGR` for inverted/wrong colors, or a specific row-byte transcription error in `display_font.c` for a garbled single character.
